
public class Arithmetic {

    public static void main(String[] args) {

        int x = 10;
        int y = 2;
        int z;

        //Arithmetic Operators - for basic solving
        //(+, -, *, /, %)

        //z = x + y;
        //z = x - y;
        //z = x * y;
        //z = x / y;
        //z = x % y;

        //Augmented Assignment Operators - storing and solving inside the same variable
        //(+=, -+, *=, /=, %=)

        //x = x + y   ->   x +=y
        //x = x - y   ->   x -=y
        //x = x * y   ->   x *=y
        //x = x / y   ->   x /=y
        //x = x % y   ->   x %=y

        //Increment and Decrement Operators
        //(++, --)

        //x++   ->   Increments/adds 1
        //x--   ->   Decrements/minus 1

        //Order of Operations (P-E-M-D-A-S)
        double result = 3 + 4 * (7 - 5) / 2.0;

        System.out.println(result);
    }
}
